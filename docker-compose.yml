services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "8787:8787"
    environment:
      PORT: 8787
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - server
